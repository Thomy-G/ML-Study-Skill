# ✍️ 2025 Moed B — Answers Template | דף תשובות — מועד ב׳ 2025

**Student Name / שם הסטודנט/ית:**  
**Date / תאריך:**  

---

## 🔹 Part A: True/False Questions / חלק א': שאלות נכון/לא נכון

### Question 1 / שאלה 1
* **a.** [True / False] — *Reasoning / נימוק:*
* **b.** [True / False] — *Reasoning / נימוק:*
* **c.** [True / False] — *Reasoning / נימוק:*
* **d.** [True / False] — *Reasoning / נימוק:*

### Question 2 / שאלה 2
* **a.** [True / False] — *Reasoning / נימוק:*
* **b.** [True / False] — *Reasoning / נימוק:*
* **c.** [True / False] — *Reasoning / נימוק:*

---

## 🔹 Part B: Open Questions / חלק ב': שאלות פתוחות

### Question 3: Linear Regression Models / שאלה 3: מודלי רגרסיה לינארית
* **a.** MSE linear regression formulation, invertibility condition & closed form:
  ```text
  
  ```
* **b.** Derivation of optimal $w_1$ given fixed $w_2$:
  ```text
  
  ```
* **c.** Model A vs Model B preference conditions:
  ```text
  
  ```
* **d.** Optimal parameter set counts for Model B and Model C:
  ```text
  
  ```
* **e.** Generalization error comparison between Model B and Model C:
  ```text
  
  ```

---

### Question 4: Binary Classification & Soft-SVM / שאלה 4: סיווג בינארי ו-Soft-SVM
* **a.** Soft-SVM optimization formulation & parameter explanations:
  ```text
  
  ```
* **b.** Orthonormal linear separability proof for $\mathbf{w} = \sum y_j \mathbf{x}_j$:
  ```text
  
  ```
* **c.** Soft-SVM zero training error proof for $C = \|\mathbf{w}^*\|^2$:
  ```text
  
  ```

---

### Question 5: Non-linear Perceptrons & Kernels / שאלה 5: פרספטרון לא-לינארי וגרעינים
* **a.** Explicit feature mapping derivation $\phi(\mathbf{x}) \in \mathbb{R}^6$ & FLOP savings:
  ```text
  
  ```
* **b.** Standard Perceptron & Hinge loss SGD pseudocode:
  ```python
  
  ```
* **c.** High-dimensional feature space Perceptron & dual weight proof:
  ```text
  
  ```
* **d.** Full Kernel Perceptron algorithm pseudocode:
  ```python
  
  ```
