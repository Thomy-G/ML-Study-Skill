# ✍️ 2022 Moed B — Answers Template | דף תשובות — מועד ב׳ 2022

**Student Name / שם הסטודנט/ית:**  
**Date / תאריך:**  

---

## Question: 2-Layer MLP Backpropagation & Loss Computation
## שאלה: חישוב קדימה, הסתברויות Softmax ומפסיד NLL ברשת ניורונים

### Part A: Hidden Layer Activation $h$ / סעיף א': אקטיבציית השכבה הנסתרת
*Show calculation for $z_1$ and $h = \sigma(z_1)$ / הציגי חישוב עבור $z_1$ ו-$h$:*

```text

```

### Part B: Logits Vector $\mathbf{z}$ / סעיף ב': וקטור ה-Logits
*Compute vector $\mathbf{z} \in \mathbb{R}^3$ / חשבי את ה-Logits:*

```text

```

### Part C: Softmax Probabilities $\hat{\mathbf{p}}$ / סעיף ג': הסתברויות Softmax
*Compute Softmax vector $\hat{\mathbf{p}}$ / חשבי את ה-Softmax:*

```text

```

### Part D: Final NLL Loss $\mathcal{L}$ / סעיף ד': פונקציית הפסד NLL
*Compute $\mathcal{L} = -\ln(p_3)$ / חשבי את הפסד NLL הסופי:*

```text

```
